name: Build MyCAD Windows

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        shell: powershell
        run: Write-Host "MyCAD Windows build workflow connected."
